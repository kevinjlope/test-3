version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - flower_data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=/app/data/products.db
    restart: always

volumes:
  flower_data:
